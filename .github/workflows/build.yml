
name: Angular build Pipeline

on: 
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
      
jobs:
  web-deploy:
    name: Build Angular App
    runs-on: ubuntu-latest

    steps:
    - run: echo "Build project"
